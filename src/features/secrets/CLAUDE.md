# Feature : Les Secrets

Rôle : fulgurances capturées — pensées brèves, révélations, intuitions.

## Composants à créer
- `SecretFeed.tsx` — flux vertical des fulgurances
- `SecretCard.tsx` — carte compacte (texte + date + domaine optionnel)
- `CaptureBar.tsx` — barre fixe en bas, texte + Enter, zéro friction

## Règle UX
Création ultra-rapide : focus auto sur `CaptureBar`, Enter soumet.
Pas de titre obligatoire, pas de Domaine obligatoire à la création.
